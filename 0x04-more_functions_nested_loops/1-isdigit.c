#include "main.h"

/**
 * _isdigit - function that checks for a digit
 * @c: char to check
 * Return: 1 (Achieved) else return 0
 */

int _isupper(int c);
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
}
