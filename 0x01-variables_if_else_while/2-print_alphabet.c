#include <stdio.h>
/**
 * main - A program that prints an alphabet in lowercase using putchar function
 * Return: 0 (Achieved)
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
		return (0);
}
