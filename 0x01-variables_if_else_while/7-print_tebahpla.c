#include <stdio.h>
/**
 * main - A program that prints the lowercase alphabet in reverse using putchar
 * Return: 0 (Achieved)
 */

int main(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}
