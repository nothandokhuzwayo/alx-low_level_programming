#ifndef _FUNCTION_LIKE_MACRO_H
#define _FUNCTION_LIKE_MACRO_H

/**
 * file: 3-function_like_macro.h
 * @x: input
 * description: that computes the absolute value of a specified number
 */
#define ABS(x) ((x) < (0) ? -(x) : (x))

#endif
